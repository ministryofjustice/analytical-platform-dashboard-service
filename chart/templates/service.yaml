apiVersion: v1
kind: Service
metadata:
  name: {{ include "analytical-platform-dashboard-service.fullname" . }}
  labels:
    {{- include "analytical-platform-dashboard-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.app.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "analytical-platform-dashboard-service.selectorLabels" . | nindent 4 }}
